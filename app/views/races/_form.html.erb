<%= form_with(model: race, local: true) do |form| %>
  <% if race.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(race.errors.count, "error") %> prohibited this race from being saved:</h2>

      <ul>
      <% race.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="box-body">
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, id: :race_name, class:"form-control", disabled:false %>
    </div>
  </div><!-- /.box-body -->
  <div class="box-footer">
    <%= link_to 'Abbrechen', races_path, :class => 'btn btn-default btn-flat btn-sm pull-left'%>
    <%= form.submit 'Speichern', :class => "btn btn-success btn-flat pull-right"%>
  </div>
<% end %>
