<%= form_with(model: persontype, local: true) do |form| %>
  <% if persontype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(persontype.errors.count, "error") %> prohibited this persontype from being saved:</h2>

      <ul>
      <% persontype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="box-body">
    <div class="field">
      <%= form.label :persontype %>
      <%= form.text_field :persontype, class:"form-control" %>
    </div>
  </div><!-- /.box-body -->
  <div class="box-footer">
    <%= link_to 'Abbrechen', persontypes_path, :class => 'btn btn-default btn-flat btn-sm pull-left'%>
    <%= form.submit 'Speichern', :class => "btn btn-success btn-flat pull-right"%>
  </div>
<% end %>
